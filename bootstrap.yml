---
- name: Shaun's Auto-Ricing Script (SARS)
  hosts: localhost
  connection: local
  vars:
    user: smoate


  roles:
    - role: install-shell
    - role: install-graphical-ui
    - role: install-dotfiles
    - role: install-zettel
    - role: post-cleanup
