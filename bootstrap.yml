---
- name: Shaun's Auto-Ricing Script (SARS)
  hosts: localhost
  connection: local
  vars:
    user: smoate


  roles:
    - role: install-core
    - role: install-dotfiles
    - role: install-shell
    - role: install-zettel
