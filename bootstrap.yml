---
- name: Bootstrap-Script
  hosts: localhost
  connection: local
  vars:
    user: user

  roles:
    - role: ssh
    - role: fonts
    - role: oh-my-zsh

    - role: cmake
    - role: sqlite

    - role: neovim
    - role: emacs

    - role: dotfiles
    - role: finalize
